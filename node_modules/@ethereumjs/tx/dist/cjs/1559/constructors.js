"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createFeeMarket1559Tx = createFeeMarket1559Tx;
exports.create1559FeeMarketTxFromBytesArray = create1559FeeMarketTxFromBytesArray;
exports.createFeeMarket1559TxFromRLP = createFeeMarket1559TxFromRLP;
const rlp_1 = require("@ethereumjs/rlp");
const util_1 = require("@ethereumjs/util");
const types_ts_1 = require("../types.js");
const internal_ts_1 = require("../util/internal.js");
const tx_ts_1 = require("./tx.js");
/**
 * Instantiate an EIP-1559 fee-market transaction from a plain data object.
 *
 * `chainId` defaults from {@link TxOptions.common} when omitted.
 *
 * @throws If fee or value fields overflow or are non-numeric
 * @throws If gas limit or nonce exceed EIP bounds
 * @throws If init code size exceeds EIP-3860 on contract-creation txs
 */
function createFeeMarket1559Tx(txData, opts = {}) {
    return new tx_ts_1.FeeMarket1559Tx(txData, opts);
}
/**
 * Instantiate an EIP-1559 transaction from devp2p byte-array encoding.
 *
 * Format: `[chainId, nonce, maxPriorityFeePerGas, maxFeePerGas, gasLimit, to, value, data,
 * accessList, signatureYParity, signatureR, signatureS]`
 *
 * @throws If the values array length is not 9 (unsigned) or 12 (signed)
 * @throws If `chainId` or signature fields are nested arrays
 * @throws If numeric fields contain leading zeroes
 * @throws If constructor validation fails (see {@link createFeeMarket1559Tx})
 */
function create1559FeeMarketTxFromBytesArray(values, opts = {}) {
    if (values.length !== 9 && values.length !== 12) {
        throw (0, util_1.EthereumJSErrorWithoutCode)('Invalid EIP-1559 transaction. Only expecting 9 values (for unsigned tx) or 12 values (for signed tx).');
    }
    const [chainId, nonce, maxPriorityFeePerGas, maxFeePerGas, gasLimit, to, value, data, accessList, v, r, s,] = values;
    (0, internal_ts_1.validateNotArray)({ chainId, v });
    (0, util_1.validateNoLeadingZeroes)({ nonce, maxPriorityFeePerGas, maxFeePerGas, gasLimit, value, v, r, s });
    return new tx_ts_1.FeeMarket1559Tx({
        chainId: (0, util_1.bytesToBigInt)(chainId),
        nonce,
        maxPriorityFeePerGas,
        maxFeePerGas,
        gasLimit,
        to,
        value,
        data,
        accessList: accessList ?? [],
        v: v !== undefined ? (0, util_1.bytesToBigInt)(v) : undefined, // EIP2930 supports v's with value 0 (empty Uint8Array)
        r,
        s,
    }, opts);
}
/**
 * Instantiate an EIP-1559 transaction from RLP-serialized bytes.
 *
 * Format: `0x02 || rlp([chainId, nonce, maxPriorityFeePerGas, maxFeePerGas, gasLimit, to, value, data,
 * accessList, signatureYParity, signatureR, signatureS])`
 *
 * @throws If the leading type byte is not `0x02`
 * @throws If RLP decode result is not an array
 * @throws If decoded values fail {@link create1559FeeMarketTxFromBytesArray} checks
 */
function createFeeMarket1559TxFromRLP(serialized, opts = {}) {
    if ((0, util_1.equalsBytes)(serialized.subarray(0, 1), (0, internal_ts_1.txTypeBytes)(types_ts_1.TransactionType.FeeMarketEIP1559)) === false) {
        throw (0, util_1.EthereumJSErrorWithoutCode)(`Invalid serialized tx input: not an EIP-1559 transaction (wrong tx type, expected: ${types_ts_1.TransactionType.FeeMarketEIP1559}, received: ${(0, util_1.bytesToHex)(serialized.subarray(0, 1))}`);
    }
    const values = rlp_1.RLP.decode(serialized.subarray(1));
    if (!Array.isArray(values)) {
        throw (0, util_1.EthereumJSErrorWithoutCode)('Invalid serialized tx input: must be array');
    }
    return create1559FeeMarketTxFromBytesArray(values, opts);
}
//# sourceMappingURL=constructors.js.map